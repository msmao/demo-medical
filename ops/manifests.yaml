apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: demo-argocd-test
  name: demo-argocd-test
  namespace: uat
spec:
  minReadySeconds: 60
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: demo-argocd-test
  template:
    metadata:
      labels:
        app: demo-argocd-test
    spec:
      containers:
        - name: demo-argocd-test
          image: gcr.io/done-280702/demo-cloudbuild-test:uat-demo-6753e80f5197baec3c543cb5215b923f68efb8b9
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: tcp-8080
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo-argocd-test
  name: demo-argocd-test
  namespace: uat
spec:
  ports:
    - name: tcp-8080
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: demo-argocd-test
  sessionAffinity: None
  type: NodePort